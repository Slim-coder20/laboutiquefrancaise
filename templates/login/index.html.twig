{% extends 'base.html.twig' %}
{% block body %}
<div class="container my-5">
<h1 class="text-danger">Connexion</h1>
{% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
 {% endif %}

<form action="{{ path('app_login') }}" method="post">
        <label for="username" class="my-3 text-primary">Votre email</label>
        <input class="form-control " type="text" id="username" name="_username" value="{{ last_username }}" required>

        <label for="password" class="my-3 text-primary">Votre mot de passe</label>
        <input class="form-control " type="password" id="password" name="_password" required>

        
        <input type="hidden" name="_target_path" value="{{ path('app_account')}}">

        <button class="btn btn-primary my-3" type="submit">Me connecter</button>
 </form>
 <br>
        Vous n'avez pas de compte <a href="{{ path('app_register')}}"> Inscrivez-vous</a><br>
        <a href="{{ path('app_password')}}">Mot de passe oublié ?</a>
</div>
{% endblock %}

