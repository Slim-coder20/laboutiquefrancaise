{% extends 'base.html.twig' %}

{% block body %}
	<div class="container my-5">
		<h1 class="mb-4 text-center font-weight-bold ">Ma commande</h1>

		<div class="card shadow-sm p-4">
			
			<div class="border p-3 rounded">
				{{ form_start(deliveryForm) }}
                    {{form_row(deliveryForm.carriers)}}
                        <a href="{{ path('app_account_adress_form')}}" class=" btn btn-success w-100">Ajouter une nouvelle adresse de livraison</a>
                        <hr>
                    {{form_row(deliveryForm.addresses)}}

				{{ form_end(deliveryForm) }}
				</div>

			

				

			</div>
		</div>
	</div>
{% endblock %}
